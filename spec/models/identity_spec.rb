require 'spec_helper'
require 'ostruct'

describe Identity do
  # before do
    # @auth = OmniAuth.mock_auth
  # end
  # describe "::find_with_omniauth" do
    # it "finds the identity if it exists" do
      # identity = create :identity
      # expect(Identity.find_with_omniauth(OpenStruct.new(@auth))).to eq identity
    # end
  # end

  # describe "::create_with_omniauth" do
    # it "creates an identity and sets the location correctly" do
      # identity = Identity.create_with_omniauth(OpenStruct.new(attributes_for(:identity, location: "Slovenia")))
      # expect(Identity.find(identity.id).location).to eq "Slovenia"
    # end
  # end
end
