.row
  = render partial: "users/user", collection: [@user], :locals => { :show_create_link => true }

- @proposals.in_groups_of(3) do |offers|
  .row
    - offers.each do |offer|
      - if !offer.nil?
        = render partial: "proposals/proposal", collection: [offer]
