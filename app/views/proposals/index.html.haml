.row
  .col-sm-2
    .h2 
      = name_for(@klass.name, plural: true).titleize
    = render "categories"
  .col-sm-10
    %table.table.proposals-table
      %tbody
        - @proposals.each do |proposal|
          - if proposal
            = render partial: "proposals/proposal", locals: { proposal: proposal, description_size: 250 }

    = paginate @proposals, theme: 'twitter-bootstrap-3'
- if @proposals.empty?
  %div.nothing-here
    There's nothing in this category. Sorry!
