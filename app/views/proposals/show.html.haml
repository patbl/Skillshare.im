%h2= @proposal.title
%p= @proposal.description
%p= @proposal.location
= link_to 'Edit', edit_user_proposal_path(@proposal.user, @proposal)
= link_to 'Delete', [@proposal.user, @proposal], method: :delete, confirm: "Are you sure you want to delete this?"
