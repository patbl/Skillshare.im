= render "shared/categories"

%table.table.proposals-table
  %thead
    %tr
      %th
      %th= name_for(@klass, capitalized: true, plural: true)
      %th Location
      %th Type
  %tbody
    - @proposals.each do |proposal|
      - if proposal
        = render partial: "proposals/proposal", locals: { proposal: proposal }

= paginate @proposals, theme: 'twitter-bootstrap-3'
- if @proposals.empty?
  %div.nothing-here
    There's nothing in this category. Sorry!
