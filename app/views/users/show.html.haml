= title @user.name

.row
  = render @user

%table.table.proposals-table
  %thead
    %tr
      %th 
      %th Description
      %th Location
      %th Type
  %tbody
    = render partial: "proposals/proposal", collection: @proposals
